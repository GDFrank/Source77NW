# NT — placeholder

Reserved for the `Source77NW.NT` library project (NT/Windows-specific
sources). When files arrive, this folder gets a `Source77NW.NT.csproj`
beside the others in `src/` and an entry in `LIBS.slnx`. This file
exists so git keeps the empty folder.
