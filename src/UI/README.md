# UI — placeholder

Reserved for the `Source77NW.UI` library project. When files arrive,
this folder gets a `Source77NW.UI.csproj` beside the others in `src/`
and an entry in `LIBS.slnx`. This file exists so git keeps the empty
folder.
